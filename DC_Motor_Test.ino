
#define enA 9
#define in1 6
#define in2 7
void setup() {
  pinMode(enA, OUTPUT);
  pinMode(in1, OUTPUT);
  pinMode(in2, OUTPUT);
  
  Serial.begin(9600);
}

void loop() {
 while(Serial.avail
 // int pwmOutput = Serial.parseInt(); // Map the potentiometer value from 0 to 255
  
  digitalWrite(in1, LOW);
  digitalWrite(in2, HIGH);


 

}
